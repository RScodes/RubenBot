var Discord= require("discord.js")

const client = new Discord.Client();

bot.on("message", function(message)
(
  if(message.content=== "substitute")
  (
  bot.reply(message, "hello substitute");
 )
  ));
  bot.loginWithToken("");
